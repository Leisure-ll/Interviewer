[project]
name = "ai-interview-runtime"
version = "0.1.0"
description = "Domain-specific Interview Agent Runtime without LangGraph."
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio"]

[tool.pytest.ini_options]
testpaths = ["tests"]
